using UnityEngine;
using System.Collections;

public class PlayerSpawn : MonoBehaviour {

	int charSpawn;
	public GameObject[] character;
//	CharacterChange change = this.GetComponent<CharacterChange> ();


	// Use this for initialization
	void Start () {
//		charSpawn = change.selectedCharacter (characterSelected);

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
